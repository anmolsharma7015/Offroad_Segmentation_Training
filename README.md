# Offroad_Segmentation_Training
This repository implements a semantic segmentation pipeline leveraging Meta's DINOv2 self-supervised ViT backbone. It features a custom ConvNeXt-style segmentation head designed to process patch tokens into high-resolution semantic masks.
